import React, { memo } from 'react';
import ListCard from '../components/ListCard';



const Dashboard = () => {
  return (
    <div>
      <ListCard />
    </div>
  );
};

export default memo(Dashboard);
